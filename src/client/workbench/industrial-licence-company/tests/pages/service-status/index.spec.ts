import index from '../../../pages/service-status';

describe('individual-industrial-licence/pages/service-status', () => {
  it('should return object ', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
