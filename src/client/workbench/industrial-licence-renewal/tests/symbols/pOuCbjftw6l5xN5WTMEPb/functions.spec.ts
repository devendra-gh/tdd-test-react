/* eslint-disable camelcase */
import { f1_visible } from '../../../symbols/pOuCbjftw6l5xN5WTMEPb/functions';

describe('Symbols function', () => {
  it('should return object ', () => {
    const props = {
      showSideBar: true,
    };
    f1_visible(props);
    expect(f1_visible).toBeInstanceOf(Object);
  });
});
