import index from '../../../pages/went-wrong';

describe('individual-industrial-licence/pages/went-wrong', () => {
  it('should return object ', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
