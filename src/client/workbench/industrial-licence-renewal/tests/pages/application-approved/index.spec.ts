import index from '../../../pages/application-approved';

describe('individual-industrial-licence/pages/application-approved', () => {
  it('should return object ', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
