import index from '../../../pages/continue-process';

describe('individual-industrial-licence/pages/continue-process', () => {
  it('should return object ', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
