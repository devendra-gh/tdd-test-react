import index from '../../../pages/application-inprogress';

describe('individual-industrial-licence/pages/application-inprogress', () => {
  it('should return object ', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
