import index from '../../../pages/licence-issued';

describe('individual-industrial-licence/pages/licence-issued', () => {
  it('should return object ', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
