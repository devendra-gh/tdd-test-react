import index from '../../../pages/choose-economic-name';

describe('individual-industrial-licence/pages/choose-economic-name', () => {
  it('should return object ', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
