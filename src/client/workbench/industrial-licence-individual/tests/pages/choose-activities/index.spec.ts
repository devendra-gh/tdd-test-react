import index from '../../../pages/choose-activities';

describe('individual-industrial-licence/pages/choose-activities', () => {
  it('should return object ', () => {
    expect(index).toBeInstanceOf(Object);
  });
});
