// // import errorPage from './index';

// jest.mock('../../utils/common', () => ({
//   REQUIRE_SOP3: jest.fn().mockReturnValue({}),
// }));

// describe('Error/index', () => {
it('test', () => {
  expect(1).toBe(1);
});
//   // it('should export routes', () => {
//   //   expect(errorPage).toBeInstanceOf(Object);
//   // });

//   // it('Check steps', () => {
//   //   errorPage[0].state.mapState.forEach((item: any) => {
//   //     if (typeof item === 'object') {
//   //       item.steps({ renewlicenceSteps: 'renewlicenceSteps' });
//   //     }
//   //   });
//   // });
// });

// import config from './index';

// describe('Error page', () => {
//   it('should return buttons on page init', () => {
//     const page = config[0];
//     const { buttons } = page.props;
//     buttons.forEach(button => button.onClick);
//     expect(Array.isArray(buttons)).toBeTruthy();
//   });
// });
