// import React from 'react';
// import { act, cleanup, render } from '@testing-library/react';
// import { MemoryRouter } from 'react-router';
// import { Viewport, ViewportProvider } from '@tamm/ui-lib-v2-viewport';
// import Component from './index';

describe('client/components/workbench/RelevantEntity', () => {
  // let props: any;

  beforeEach(() => {
    // props = {
    //   locale: 'en',
    //   i18n: jest.fn(i => i),
    //   title: 'Entity',
    //   entities: [
    //     {
    //       naturalHeight: '',
    //       loadServices: {},
    //       loadKiosks: {},
    //     },
    //   ],
    //   loadServices: jest.fn(),
    //   loadKiosks: jest.fn(),
    //   naturalHeight: jest.fn(),
    // };
  });

  // afterEach(cleanup);

  // it('should render with all props', async () => {
  //   await act(async () => {
  //     const { getByText } = render(
  //       <MemoryRouter>
  //         <ViewportProvider>
  //           <Viewport sm md lg xl>
  //             <Component {...props} />
  //           </Viewport>
  //         </ViewportProvider>
  //       </MemoryRouter>,
  //     );

  //     getByText('Entity');
  //   });
  // });

  it('test(', () => {
    expect(1).toBe(1);
  });
});
